---
phase: 02-skill-wiring
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - skills/feature-implement/SKILL.md
  - skills/code-uplift/SKILL.md
  - tests/bats/int-finish-tail.bats
autonomous: true
requirements: [REQ-03, REQ-09]

must_haves:
  truths:
    - "The feature-implement finish tail (Step 6) AND the code-uplift finish tail (Step 4) each name a learnings step invoking `scripts/gsd/learnings-harvest.sh`; because code-uplift Step 4 currently says only 'Identical to feature-implement Step 6', it must add the LITERAL `learnings-harvest.sh` token so its own grep-pin holds (AC-003, REQ-03 wiring half, INT-004)"
    - "The learnings step is fail-soft: `learnings-harvest.sh` always exits 0 and prints `<N> harvested` — it never blocks ship (AC-003; matches the frozen lever's contract, 01-04-SUMMARY)"
    - "code-uplift gains a `--slop-only <diff-base>` fast path documented as an optional finish-tail step 0: a green-baseline WALL (refuse to edit on a failing suite), deletion-first, scope limited to the files in the diff, re-run the suite after, and report the net line delta (AC-009, REQ-09)"
    - "`--slop-only` empty-diff case is a no-op success reporting `0 files in scope` (EDGE-008)"
    - "tests/bats/int-finish-tail.bats grep-pins are RED-first: assert `learnings-harvest.sh` present in BOTH feature-implement and code-uplift SKILL.md, and `--slop-only` present in code-uplift SKILL.md, all failing before the prose edits"
    - "Version bumps: feature-implement 2.3.0 → 2.4.0; code-uplift 1.0.0 → 1.1.0"
    - "No lever is changed — learnings-harvest.sh is invoked, not edited; the deslop fast path is skill prose (an invocation MODE of /code-uplift), not a new script (hard constraint: prose edits only)"
  artifacts:
    - path: "tests/bats/int-finish-tail.bats"
      provides: "INT-004 grep-pin (learnings-harvest.sh in both finish tails) + --slop-only grep-pin in code-uplift"
      contains: "learnings-harvest.sh"
    - path: "skills/feature-implement/SKILL.md"
      provides: "Step 6 finish tail gains a fail-soft learnings-harvest step; version 2.4.0"
      contains: "learnings-harvest.sh"
    - path: "skills/code-uplift/SKILL.md"
      provides: "Step 4 finish tail names learnings-harvest.sh; new --slop-only optional step-0 fast path; version 1.1.0"
      contains: "learnings-harvest.sh"
  key_links:
    - from: "skills/feature-implement/SKILL.md"
      to: "scripts/gsd/learnings-harvest.sh"
      via: "finish-tail step invokes the fail-soft harvester (gbrain-or-archive), prints harvested count"
      pattern: "learnings-harvest.sh"
    - from: "skills/code-uplift/SKILL.md"
      to: "scripts/gsd/learnings-harvest.sh"
      via: "finish-tail step names the same harvester; --slop-only adds a deletion-first fast path"
      pattern: "learnings-harvest.sh"
  prohibitions:
    - "Does NOT change scripts/gsd/learnings-harvest.sh — the lever is frozen (invoke only)"
    - "Does NOT create a new script for --slop-only — it is a documented invocation mode of /code-uplift (skill prose)"
    - "Does NOT weaken the green-baseline WALL — --slop-only refuses to edit on a failing baseline suite (AC-009)"
    - "Touches ONLY skills/feature-implement/SKILL.md, skills/code-uplift/SKILL.md, tests/bats/int-finish-tail.bats"
---

<objective>
Wire the learnings-harvest lever into both finish tails and add the deslop fast path:
(1) the feature-implement Step-6 and code-uplift Step-4 finish tails each gain a
fail-soft `scripts/gsd/learnings-harvest.sh` step so each finished run's debugging
lessons are persisted automatically (US2, AC-003), and (2) code-uplift gains a
`--slop-only <diff-base>` optional step-0 fast path — a green-baseline-walled,
deletion-first, diff-scoped cleanup that reports the net line delta (US6, AC-009).
Both grep-pinned RED-first by a new bats file.

Purpose: the next spec's planner benefits from this run's learnings without the
operator remembering a manual step; and reviewers see a smaller, slop-free diff.
feature-implement + code-uplift are the two finish-tail-owning skills, so they pair
in one plan (no file overlap with 02-01 or 02-03).

Output: tests/bats/int-finish-tail.bats (new), skills/feature-implement/SKILL.md
(learnings step, v2.4.0), skills/code-uplift/SKILL.md (learnings step + --slop-only,
v1.1.0).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@specs/003-orchestration-hardening/spec.md
@specs/003-orchestration-hardening/plan.md
@skills/feature-implement/SKILL.md
@skills/code-uplift/SKILL.md
@scripts/gsd/learnings-harvest.sh
@.planning/phases/01-levers/01-04-SUMMARY.md
@tests/bats/setup-install.bats

<interfaces>
<!-- Executor: exact lever contract the prose must match. Lever is FROZEN — invoke, do not edit. -->

learnings-harvest.sh (scripts/gsd/learnings-harvest.sh — frozen lever):
  Usage: learnings-harvest.sh [<planning-dir>]   (default: .planning)
  Reads .planning/**/learnings*.jsonl; writes each valid entry to gbrain when healthy
    (`command -v gbrain` + `env -u DATABASE_URL gbrain doctor`), else appends atomically to
    .feature-fix-swarm/learnings-archive.jsonl. Skips malformed JSONL lines (counts them).
  stdout: "<N> harvested" (and WARN lines on stderr for malformed/unreachable-backend).
  exit: ALWAYS 0 — fail-soft, never blocks ship (AC-003). Invoke as:
    bash scripts/gsd/learnings-harvest.sh

--slop-only contract (AC-009 — SKILL PROSE, no new script):
  Invocation:  /code-uplift <path> --slop-only <diff-base>
  Green-baseline WALL: run the suite first; if it is NOT green, REFUSE to edit and report the
    failing baseline (spec Scenario "deslop refuses without green baseline").
  Deletion-first; scope limited to files present in `git diff <diff-base>` (diff files only).
  Re-run the suite AFTER; report the net line delta (must be a strictly smaller / non-positive
    delta on success). EDGE-008: <diff-base> == HEAD (empty diff) -> no-op success, "0 files in scope".
  Documented as optional finish-tail step 0 (before review-gate sees the diff).

Finish-tail anchors:
  feature-implement SKILL.md "### Step 6: Finish tail" (~126-141): steps are
    1 browser-gate -> 2 qa-coverage -> 3 review-gate/ship/canary. Add the learnings step here.
  code-uplift SKILL.md "### Step 4: Finish tail" (~115-118): currently "Identical to
    /feature-implement Step 6" — add the LITERAL learnings-harvest.sh mention so its grep-pin holds.
    Step 0 (~47-58) is where the --slop-only fast path section slots in (before/around baseline).

Grep-pin convention: mirror tests/bats/setup-install.bats — a bats @test that greps a static
string out of a committed SKILL.md and asserts presence. No script execution.
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED→GREEN — learnings-harvest step in both finish tails</name>
  <files>tests/bats/int-finish-tail.bats, skills/feature-implement/SKILL.md, skills/code-uplift/SKILL.md</files>
  <read_first>
    - skills/feature-implement/SKILL.md — "### Step 6: Finish tail" (~126-141)
    - skills/code-uplift/SKILL.md — "### Step 4: Finish tail" (~115-118)
    - scripts/gsd/learnings-harvest.sh (the frozen lever's usage + always-exit-0 contract)
    - specs/003-orchestration-hardening/spec.md — AC-003, EDGE-006; plan.md — INT-004
    - tests/bats/setup-install.bats (grep-pin @test shape)
  </read_first>
  <behavior>
    - int-finish-tail.bats asserts `skills/feature-implement/SKILL.md` contains the literal `learnings-harvest.sh` (INT-004, feature-implement half)
    - int-finish-tail.bats asserts `skills/code-uplift/SKILL.md` contains the literal `learnings-harvest.sh` (INT-004, code-uplift half)
    - both pins fail RED before the edits (neither SKILL.md names the script today)
  </behavior>
  <action>
    Write tests/bats/int-finish-tail.bats FIRST with two @test blocks (grep the `learnings-harvest.sh`
    literal from each SKILL.md, mirror setup-install.bats). Confirm RED.

    Then edit skills/feature-implement/SKILL.md Step 6: add a learnings step to the finish-tail
    sequence — `bash scripts/gsd/learnings-harvest.sh` persists this run's `.planning/**/learnings*.jsonl`
    to gbrain-or-archive and prints `<N> harvested`; state it is FAIL-SOFT (always exit 0, never blocks
    ship — AC-003). Place it as a distinct numbered step in the tail (after review-gate/ship is fine —
    learnings are a run-end capture) and mention its count belongs in the Step-7 report. Bump
    feature-implement `version:` 2.3.0 → 2.4.0.

    Then edit skills/code-uplift/SKILL.md Step 4: it currently only says "Identical to
    /feature-implement Step 6" — add one sentence that NAMES the step literally, e.g. "including the
    fail-soft `scripts/gsd/learnings-harvest.sh` learnings step", so code-uplift's own grep-pin holds.
    (code-uplift's version bump happens in Task 2, its final touch.) Prose only — do NOT edit the lever.
  </action>
  <verify>
    <automated>bats tests/bats/int-finish-tail.bats && grep -q 'learnings-harvest.sh' skills/feature-implement/SKILL.md && grep -q 'learnings-harvest.sh' skills/code-uplift/SKILL.md</automated>
  </verify>
  <done>Both learnings @tests pass; `learnings-harvest.sh` literal present in both SKILL.md files; feature-implement version is 2.4.0; the step is documented fail-soft.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: RED→GREEN — code-uplift --slop-only fast path</name>
  <files>tests/bats/int-finish-tail.bats, skills/code-uplift/SKILL.md</files>
  <read_first>
    - skills/code-uplift/SKILL.md — Invocation block (~36-43) and "### Step 0: Resolve target + baseline" (~47-58)
    - specs/003-orchestration-hardening/spec.md — AC-009, EDGE-008, Scenarios "deslop pass shrinks the diff" / "deslop refuses without green baseline"
    - plan.md — Phase 2 "code-uplift --slop-only [AC-009, OMC ai-slop-cleaner]"
  </read_first>
  <behavior>
    - int-finish-tail.bats asserts `skills/code-uplift/SKILL.md` contains the literal `--slop-only`
    - int-finish-tail.bats asserts the green-baseline WALL is documented — a grep for the refuse-on-failing-baseline wording (e.g. `baseline` in the --slop-only section)
    - both new pins fail RED before the edit
  </behavior>
  <action>
    Append two @test blocks to tests/bats/int-finish-tail.bats: a `--slop-only` literal pin and a
    green-baseline-wall pin (grep for the refusal/`baseline` wording). Confirm RED. (Note: `--slop-only`
    is the acceptance-test token from ROADMAP criterion 2; assert its PRESENCE.)

    Then edit skills/code-uplift/SKILL.md: add `/code-uplift <path> --slop-only <diff-base>` to the
    Invocation block, and add a section (an optional finish-tail step 0, e.g. `### Step 0b: --slop-only
    fast path (optional)`) documenting the contract: (1) green-baseline WALL — run the suite first and
    REFUSE to edit if it is not green, reporting the failing baseline; (2) deletion-first; (3) scope
    limited to files in `git diff <diff-base>` only; (4) re-run the suite after and report the net line
    delta (success = a strictly smaller diff); (5) EDGE-008 — `<diff-base>` == HEAD (empty diff) is a
    no-op success reporting `0 files in scope`. Frame it as running BEFORE review-gate sees the diff.
    Bump code-uplift `version:` 1.0.0 → 1.1.0. Prose only — NO new script.
  </action>
  <verify>
    <automated>bats tests/bats/int-finish-tail.bats && grep -q -- '--slop-only' skills/code-uplift/SKILL.md && grep -q 'learnings-harvest.sh' skills/code-uplift/SKILL.md</automated>
  </verify>
  <done>All int-finish-tail.bats @tests pass; `--slop-only` + `learnings-harvest.sh` literals present in code-uplift SKILL.md; green-baseline wall documented; code-uplift version is 1.1.0.</done>
</task>

</tasks>

<artifacts_produced>
New/modified this plan (exclude from cross-plan source-drift verification):
- `tests/bats/int-finish-tail.bats` (new — INT-004 learnings pins + --slop-only pins)
- `skills/feature-implement/SKILL.md` (modified — learnings step, version 2.4.0)
- `skills/code-uplift/SKILL.md` (modified — learnings step + --slop-only fast path, version 1.1.0)
- Consumes (does NOT modify): scripts/gsd/learnings-harvest.sh
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| finish-tail prose → ship flow | the learnings step must be fail-soft — a broken/unreachable memory backend must never block ship |
| --slop-only prose → source deletion | deletion-first cleanup on unproven code is a data-loss risk; the green-baseline WALL + diff-only scope bound it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-FT-01 | Denial of Service | learnings step blocking ship | medium | mitigate | invoke the frozen fail-soft lever (always exit 0); prose states it never blocks (AC-003) |
| T-02-FT-02 | Tampering | --slop-only deleting code with no safety net | high | mitigate | green-baseline WALL (refuse on failing suite) + deletion-first scope limited to diff files + suite re-run after with net-delta report (AC-009) |
| T-02-SC | Tampering | package installs | low | accept | No package installs — prose + bats grep-pins only |
</threat_model>

<verification>
- `bats tests/bats/int-finish-tail.bats` exits 0 (learnings pins + --slop-only pins)
- `grep -q 'learnings-harvest.sh' skills/feature-implement/SKILL.md && grep -q -- '--slop-only' skills/code-uplift/SKILL.md` exits 0 (ROADMAP Phase 2 criterion 2, this plan's halves)
- `grep -q 'learnings-harvest.sh' skills/code-uplift/SKILL.md` exits 0 (INT-004 code-uplift half)
- `bats tests/bats/` exits 0 (no regression)
- `python3 -m pytest -q` unchanged vs current baseline, 0 failures
- `grep -q '2.4.0' skills/feature-implement/SKILL.md && grep -q '1.1.0' skills/code-uplift/SKILL.md`
</verification>

<success_criteria>
- REQ-03 (wiring half) satisfied: both finish tails invoke learnings-harvest.sh, fail-soft.
- REQ-09 satisfied: code-uplift --slop-only documented with green-baseline wall, diff-only scope, net-delta report.
- No lever changed; no new script; only the two named SKILL.md + the bats file touched.
</success_criteria>

<output>
Create `.planning/phases/02-skill-wiring/02-02-SUMMARY.md` when done.
</output>
