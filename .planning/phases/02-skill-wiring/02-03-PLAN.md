---
phase: 02-skill-wiring
plan: 03
type: tdd
wave: 1
depends_on: []
files_modified:
  - skills/adopt-wip/SKILL.md
  - skills/preflight/SKILL.md
  - skills/fix/SKILL.md
  - tests/bats/int-consult-levers.bats
autonomous: true
requirements: [REQ-08, REQ-10, REQ-11]

must_haves:
  truths:
    - "adopt-wip Step 1 ('Confirm the worktree is stale') consults `scripts/gsd/liveness-check.sh` before declaring WIP abandoned: exit 1 (DEAD, all signals false) → adoptable; exit 0 (ALIVE, any signal holds) → WAIT/do-not-touch (AC-011 tail, REQ-11 adopt-wip half, INT-005)"
    - "preflight gains an ADVISORY harness-audit section invoking `python3 scripts/harness-audit.py` (or `--json`); it NEVER blocks preflight — a low score surfaces as an advisory finding only (AC-008 tail, REQ-08 preflight half)"
    - "fix Step 1 (diagnose) routes to `/gsd-debug` when reproduction is non-obvious, with the EXPLICIT criteria: no failing test yet AND no single-command repro (AC-010, REQ-10, INT-006); when a failing test or single-command repro exists, it stays on the current /investigate + /gsd-quick path"
    - "liveness-check invocation matches the frozen lever's CLI: `liveness-check.sh <pidfile> <state-dir> [--run-id ID] [--window-min N]`, exit 0 alive / 1 dead, LIVENESS_WINDOW_MIN default 30 (01-03-SUMMARY)"
    - "harness-audit invocation matches the frozen lever: `python3 scripts/harness-audit.py [--json]`, always exit 0, prints `HARNESS-AUDIT: score N/100` (01-05-SUMMARY)"
    - "tests/bats/int-consult-levers.bats grep-pins are RED-first: `liveness-check.sh` in adopt-wip, `harness-audit.py` in preflight, `gsd-debug` in fix — all failing before the prose edits"
    - "Version bumps: adopt-wip 1.0.0 → 1.1.0; preflight 1.0.0 → 1.1.0; fix 3.1.0 → 3.2.0"
  artifacts:
    - path: "tests/bats/int-consult-levers.bats"
      provides: "INT-005 (liveness-check.sh in adopt-wip) + harness-audit.py pin in preflight + INT-006 (gsd-debug in fix)"
      contains: "liveness-check.sh"
    - path: "skills/adopt-wip/SKILL.md"
      provides: "Step 1 consults liveness-check.sh before abandonment; version 1.1.0"
      contains: "liveness-check.sh"
    - path: "skills/preflight/SKILL.md"
      provides: "advisory harness-audit.py section, never blocks; version 1.1.0"
      contains: "harness-audit.py"
    - path: "skills/fix/SKILL.md"
      provides: "diagnose step routes to /gsd-debug on non-obvious repro; version 3.2.0"
      contains: "gsd-debug"
  key_links:
    - from: "skills/adopt-wip/SKILL.md"
      to: "scripts/gsd/liveness-check.sh"
      via: "Step 1 runs the composite liveness probe; exit 1 = abandoned/adoptable, exit 0 = alive/WAIT"
      pattern: "liveness-check.sh"
    - from: "skills/preflight/SKILL.md"
      to: "scripts/harness-audit.py"
      via: "advisory section scores the harness; never blocks preflight"
      pattern: "harness-audit.py"
    - from: "skills/fix/SKILL.md"
      to: "/gsd-debug"
      via: "diagnose routes non-obvious reproductions to the gsd scientific-method loop"
      pattern: "gsd-debug"
  prohibitions:
    - "Does NOT change scripts/gsd/liveness-check.sh or scripts/harness-audit.py — frozen levers (invoke only)"
    - "Does NOT make the harness-audit section block preflight — it is advisory, exit 0 always (AC-008)"
    - "Does NOT remove adopt-wip's existing stale-checks (mtime/process/WAIT) — liveness-check is ADDED as the composite consult, not a replacement"
    - "Touches ONLY skills/adopt-wip/SKILL.md, skills/preflight/SKILL.md, skills/fix/SKILL.md, tests/bats/int-consult-levers.bats"
---

<objective>
Wire the three remaining 'consult a lever before a decision' prose contracts:
(1) adopt-wip Step 1 consults `scripts/gsd/liveness-check.sh` before declaring WIP
abandoned, so one transient failed probe never kills a live overnight wave (US8,
AC-011 tail); (2) preflight gains an advisory `scripts/harness-audit.py` section
that scores the harness itself and never blocks (US5, AC-008 tail); (3) fix Step 1
routes non-obvious reproductions to `/gsd-debug` with explicit criteria (US7,
AC-010). All grep-pinned RED-first by a new bats file.

Purpose: an unattended run should not be abandoned on a single-signal probe, should
not start on a silently broken harness, and a hard bug should get hypothesis-driven
investigation instead of ad-hoc probing. These three are small, cohesive 'invoke a
frozen lever from skill prose' edits with no file overlap with 02-01 or 02-02.

Output: tests/bats/int-consult-levers.bats (new), and version-bumped edits to
skills/adopt-wip/SKILL.md, skills/preflight/SKILL.md, skills/fix/SKILL.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@specs/003-orchestration-hardening/spec.md
@specs/003-orchestration-hardening/plan.md
@skills/adopt-wip/SKILL.md
@skills/preflight/SKILL.md
@skills/fix/SKILL.md
@scripts/gsd/liveness-check.sh
@scripts/harness-audit.py
@.planning/phases/01-levers/01-03-SUMMARY.md
@.planning/phases/01-levers/01-05-SUMMARY.md
@tests/bats/setup-install.bats

<interfaces>
<!-- Executor: exact lever contracts the prose must match. Levers are FROZEN — invoke, do not edit. -->

liveness-check.sh (scripts/gsd/liveness-check.sh — frozen lever):
  Usage: liveness-check.sh <pidfile> <state-dir> [--run-id ID] [--window-min N]
  Composite AND-of-death: ALIVE (exit 0) iff P(pid alive) OR M(newest mtime under state-dir
    within window) OR G(gates.py ship:gsd grant in flight); DEAD (exit 1) ONLY when all three fail.
  Env: LIVENESS_WINDOW_MIN (default 30). Prints per-signal verdict lines (P=/M=/G=) then ALIVE/DEAD.
  adopt-wip consult semantics: exit 1 → abandoned, safe to adopt; exit 0 → still alive, WAIT.

harness-audit.py (scripts/harness-audit.py — frozen lever):
  Usage: python3 scripts/harness-audit.py [--json]
  Scores the ~/.claude harness 0-100 (dangling skill links, vendored-copy drift, dead model pins,
    unregistered hooks). --json → {"score":int,"findings":[{"kind","path","detail"}],"note"?}.
    Human form prints "HARNESS-AUDIT: score N/100" + one line per finding. Always exit 0 (advisory).
  EDGE-007: no ~/.claude/skills → score 100 with a "skipped: no harness dir" note (absence != drift).

fix → gsd-debug routing (AC-010 — SKILL PROSE contract, explicit criteria):
  In fix Step 1 (diagnose), route to `/gsd-debug` (gsd scientific-method loop) WHEN
    (no failing test yet) AND (no single-command repro exists). Consume its root-cause report
    before editing. Otherwise stay on the current /investigate + /gsd-quick path.

Anchors:
  adopt-wip SKILL.md Step 1 "Confirm the worktree is stale" (~25-29) — add the liveness-check consult
    alongside (not replacing) the mtime/process/WAIT checks.
  preflight SKILL.md — add an advisory harness-audit section after step 3 (~51-53) or under "## Rules".
  fix SKILL.md Step 1 "Investigate (root cause, not symptom)" (~36-40) — add the gsd-debug routing.

Grep-pin convention: mirror tests/bats/setup-install.bats — a bats @test that greps a static
string out of a committed SKILL.md and asserts presence.
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED→GREEN — adopt-wip liveness consult + preflight harness-audit advisory</name>
  <files>tests/bats/int-consult-levers.bats, skills/adopt-wip/SKILL.md, skills/preflight/SKILL.md</files>
  <read_first>
    - skills/adopt-wip/SKILL.md — Step 1 "Confirm the worktree is stale" (~25-29)
    - skills/preflight/SKILL.md — steps 1-3 (~20-53) + "## Rules" (~55-64)
    - scripts/gsd/liveness-check.sh (frozen lever CLI + exit semantics)
    - scripts/harness-audit.py (frozen lever CLI + always-exit-0 advisory contract)
    - specs/003-orchestration-hardening/spec.md — AC-011, AC-008, EDGE-005, EDGE-007; plan.md — INT-005
  </read_first>
  <behavior>
    - int-consult-levers.bats asserts `skills/adopt-wip/SKILL.md` contains the literal `liveness-check.sh` (INT-005)
    - int-consult-levers.bats asserts `skills/preflight/SKILL.md` contains the literal `harness-audit.py`
    - int-consult-levers.bats asserts the preflight section is advisory — grep for advisory/never-blocks wording so a future edit that makes it a hard gate trips the pin
    - all three pins fail RED before the edits
  </behavior>
  <action>
    Write tests/bats/int-consult-levers.bats FIRST with the adopt-wip `liveness-check.sh` pin, the
    preflight `harness-audit.py` pin, and the preflight advisory-wording pin (mirror setup-install.bats).
    Confirm RED.

    Then edit skills/adopt-wip/SKILL.md Step 1: ADD (do not replace the existing mtime/process/WAIT
    checks) a composite-liveness consult — `bash scripts/gsd/liveness-check.sh <pidfile> <state-dir>`
    decides abandonment: exit 1 (DEAD — pid gone AND mtime stale AND no in-flight ship grant) → the WIP
    is truly abandoned and adoptable; exit 0 (ALIVE — any signal holds) → WAIT, do not touch. Note
    LIVENESS_WINDOW_MIN default 30 and the accepted PID-reuse false-alive risk (EDGE-005). Bump
    adopt-wip `version:` 1.0.0 → 1.1.0.

    Then edit skills/preflight/SKILL.md: add a `### Harness audit (advisory)` section (after step 3 or
    under Rules) — `python3 scripts/harness-audit.py` scores the ~/.claude harness (dangling links,
    vendored drift, dead model pins, unregistered hooks) and surfaces a low score as an ADVISORY finding
    that NEVER blocks preflight (always exit 0; absence of the harness dir is a skip-note, not a failure —
    EDGE-007). State plainly it is advisory, not a fail-closed gate. Bump preflight `version:` 1.0.0 → 1.1.0.
    Prose only — do NOT edit either lever.
  </action>
  <verify>
    <automated>bats tests/bats/int-consult-levers.bats && grep -q 'liveness-check.sh' skills/adopt-wip/SKILL.md && grep -q 'harness-audit.py' skills/preflight/SKILL.md</automated>
  </verify>
  <done>adopt-wip + preflight pins pass; `liveness-check.sh` present in adopt-wip, `harness-audit.py` present in preflight (documented advisory/non-blocking); versions 1.1.0/1.1.0; existing stale-checks retained.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: RED→GREEN — fix diagnose routes to /gsd-debug on non-obvious repro</name>
  <files>tests/bats/int-consult-levers.bats, skills/fix/SKILL.md</files>
  <read_first>
    - skills/fix/SKILL.md — Step 1 "Investigate (root cause, not symptom)" (~36-40) and Step 2 (~42-64)
    - specs/003-orchestration-hardening/spec.md — AC-010, Scenario "hard bug routes to debug loop"; plan.md — INT-006
  </read_first>
  <behavior>
    - int-consult-levers.bats asserts `skills/fix/SKILL.md` contains the literal `gsd-debug` (INT-006)
    - int-consult-levers.bats asserts the routing CRITERIA are documented — grep for the "no failing test" / "single-command repro" criteria wording so the contract (not just the token) is pinned
    - both new pins fail RED before the edit
  </behavior>
  <action>
    Append two @test blocks to tests/bats/int-consult-levers.bats: a `gsd-debug` literal pin and a
    routing-criteria pin (grep for the repro-criteria wording). Confirm RED.

    Then edit skills/fix/SKILL.md Step 1: add the routing contract — when reproduction is non-obvious,
    specifically when (no failing test yet) AND (no single-command repro exists), route diagnosis to
    `/gsd-debug` (gsd's scientific-method hypothesis→experiment loop) and consume its root-cause report
    BEFORE editing. When a failing test or a single-command repro already exists, stay on the current
    /investigate → /gsd-quick path. Keep the existing "grep every caller / root-cause site" guidance.
    Bump fix `version:` 3.1.0 → 3.2.0. Prose only.
  </action>
  <verify>
    <automated>bats tests/bats/int-consult-levers.bats && grep -q 'gsd-debug' skills/fix/SKILL.md</automated>
  </verify>
  <done>All int-consult-levers.bats @tests pass; `gsd-debug` present in fix SKILL.md with the explicit no-failing-test-AND-no-single-command-repro criteria; fix version is 3.2.0.</done>
</task>

</tasks>

<artifacts_produced>
New/modified this plan (exclude from cross-plan source-drift verification):
- `tests/bats/int-consult-levers.bats` (new — INT-005 + harness-audit + INT-006 grep-pins)
- `skills/adopt-wip/SKILL.md` (modified — liveness consult, version 1.1.0)
- `skills/preflight/SKILL.md` (modified — advisory harness-audit section, version 1.1.0)
- `skills/fix/SKILL.md` (modified — gsd-debug routing, version 3.2.0)
- Consumes (does NOT modify): scripts/gsd/liveness-check.sh, scripts/harness-audit.py
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| adopt-wip prose → abandonment decision | a single-signal 'dead' verdict can kill a live run; the composite AND-of-death consult is the guardrail |
| preflight prose → run start | the harness audit must inform, never block — a false low score must not wall an unattended start |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-CL-01 | Denial of Service | adopt-wip abandoning a live run on a transient probe | high | mitigate | consult composite liveness-check.sh (exit 1 only when pid AND mtime AND grant all fail); exit 0 → WAIT; existing WAIT-on-doubt guidance retained (AC-011) |
| T-02-CL-02 | Denial of Service | harness-audit walling preflight on a false low score | medium | mitigate | section documented ADVISORY, invokes the always-exit-0 lever; absence of harness dir is a skip-note not a failure (EDGE-007) |
| T-02-CL-03 | Repudiation | fix editing before root cause on a hard bug | low | mitigate | non-obvious repro (no failing test AND no single-command repro) routes to /gsd-debug and consumes its root-cause report before editing (AC-010) |
| T-02-SC | Tampering | package installs | low | accept | No package installs — prose + bats grep-pins only |
</threat_model>

<verification>
- `bats tests/bats/int-consult-levers.bats` exits 0 (liveness + harness-audit + gsd-debug pins)
- `grep -q 'liveness-check.sh' skills/adopt-wip/SKILL.md && grep -q 'harness-audit.py' skills/preflight/SKILL.md && grep -q 'gsd-debug' skills/fix/SKILL.md` exits 0 (ROADMAP Phase 2 criterion 2, this plan's three halves)
- `bats tests/bats/` exits 0 (no regression)
- `python3 -m pytest -q` unchanged vs current baseline, 0 failures (no python touched)
- `grep -q '1.1.0' skills/adopt-wip/SKILL.md && grep -q '1.1.0' skills/preflight/SKILL.md && grep -q '3.2.0' skills/fix/SKILL.md`
</verification>

<success_criteria>
- REQ-11 (adopt-wip half) satisfied: adopt-wip consults liveness-check.sh before abandonment.
- REQ-08 (preflight half) satisfied: preflight advisory harness-audit section, never blocks.
- REQ-10 satisfied: fix diagnose routes to /gsd-debug on non-obvious repro with explicit criteria.
- No lever changed; only the three named SKILL.md + the bats file touched.
</success_criteria>

<output>
Create `.planning/phases/02-skill-wiring/02-03-SUMMARY.md` when done.
</output>
