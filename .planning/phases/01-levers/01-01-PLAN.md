---
phase: 01-levers
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - scripts/hooks/delegation-enforcer.sh
  - tests/bats/delegation-enforcer.bats
autonomous: true
requirements: [REQ-01, REQ-02]

must_haves:
  truths:
    - "Unpinned Agent/Task JSON on stdin + .planning/config.json overrides → same JSON with tool_input.model injected from model_overrides[subagent_type] (AC-001, PATH-001)"
    - "Agent/Task JSON already carrying tool_input.model → byte-identical stdout passthrough (AC-001)"
    - "No .planning/config.json → passthrough + stderr warn (AC-001)"
    - "config.json present but no model_overrides key → passthrough + stderr warn, treated as no-config (EDGE-002)"
    - "Non-Agent tool JSON (e.g. Bash) → byte-identical passthrough, no parse beyond tool-name check (EDGE-001)"
    - "DELEGATION_ENFORCER=off → unconditional passthrough (AC-002)"
    - "Malformed/hostile JSON on stdin → passthrough, never crash (fail-open)"
    - "Exit code is 0 in all non-usage cases — advisory injection, never blocks a spawn (AC-001)"
  artifacts:
    - path: "scripts/hooks/delegation-enforcer.sh"
      provides: "PreToolUse hook that auto-pins unpinned Agent/Task spawns from config"
      min_lines: 30
    - path: "tests/bats/delegation-enforcer.bats"
      provides: "happy + error path coverage (inject/passthrough/off/fail-open)"
      contains: "delegation-enforcer.sh"
  key_links:
    - from: "scripts/hooks/delegation-enforcer.sh"
      to: ".planning/config.json"
      via: "reads model_overrides keyed by subagent_type to resolve the pin"
      pattern: "model_overrides"
  prohibitions:
    - "Does NOT write or edit .claude/settings.json — hook registration is a consumer-repo action shipped as a docs snippet (plan.md architecture note)"
    - "Does NOT edit any skills/*/SKILL.md — enforcer wiring is Phase 2"
    - "Never emits a non-zero exit for a delegation decision — advisory only (AC-001)"
---

<objective>
Build `scripts/hooks/delegation-enforcer.sh`: a PreToolUse hook that reads Agent/Task
tool_use JSON on stdin and auto-pins an unpinned spawn to the model resolved from
`.planning/config.json` `model_overrides[subagent_type]`, emitting the (possibly
modified) JSON on stdout with exit 0 — prevention-first cost control instead of the
morning-after delegation-audit report (US1).

Purpose: unpinned sub-agent spawns drift to premium tiers and burn cost; catching them
at spawn time is the only prevention point (RESEARCH Architectural Responsibility Map).

Output: the hook script + its bats happy/error coverage.

RESOLVED DESIGN DECISION (Open Question 1 — hook stdout shape): This phase implements
the hook to satisfy the spec's own PATH-001 bats stub literally — it echoes the FULL
input envelope with `tool_input.model` injected (assertion: `jq -e '.tool_input.model
== "sonnet"'`), NOT the `hookSpecificOutput.updatedInput` wrapper. RESEARCH Open
Question 1 recommends this: Phase 1 only needs the SCRIPT correct in isolation (no live
Claude Code process in the loop). A follow-up note for Phase 2/3 (live-registration
wiring): confirm Claude Code's real `hookSpecificOutput.updatedInput` contract before
registering — do NOT attempt live-hook wiring here.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/config.json
@specs/003-orchestration-hardening/spec.md
@scripts/hooks/gsd-phase-evidence-gate.sh

<interfaces>
<!-- Executor: copy the stdin/heredoc shape from the existing hook — no exploration needed. -->

Existing PreToolUse hook pattern to copy (scripts/hooks/gsd-phase-evidence-gate.sh):
  - `set -euo pipefail` (hooks abort loudly — RESEARCH Pattern 5)
  - early bypass: `[ "${GATES_BYPASS:-0}" = "1" ] && exit 0`  (here: DELEGATION_ENFORCER=off)
  - `INPUT=$(cat)` then `python3 - "$INPUT" <<'PY' ... PY`
  - python side: `data = json.loads(sys.argv[1])` inside try/except → `sys.exit(0)` on failure
  - `ti = data.get("tool_input", {}) or {}`

Tool-input fields (VERIFIED skills/review-gate/SKILL.md:277 —
Task({ subagent_type: 'gsd-verifier', model: 'sonnet', ... })):
  - data["tool_name"] ∈ {"Agent","Task"} gates whether to act
  - ti["subagent_type"]  e.g. "gsd-verifier"
  - ti["model"]          present ⇒ already pinned ⇒ passthrough

config resolution (.planning/config.json):
  - model_overrides: { "gsd-executor": "sonnet", "gsd-verifier": "opus", ... }
  - lookup key = ti["subagent_type"]; inject as ti["model"] if found
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED — delegation-enforcer.bats covering inject / passthrough / off / fail-open</name>
  <files>tests/bats/delegation-enforcer.bats</files>
  <read_first>
    - scripts/hooks/gsd-phase-evidence-gate.sh (stdin/heredoc + fail-open pattern to mirror)
    - specs/003-orchestration-hardening/spec.md — PATH-001 stub (lines ~176-184), AC-001/002, EDGE-001/002
    - tests/bats/gsd-phase-evidence-gate.bats (bats + jq assertion convention already in this repo)
  </read_first>
  <behavior>
    - PATH-001: seed a config with model_overrides; pipe unpinned Agent JSON (subagent_type set, no model) → status 0 AND `jq -e '.tool_input.model == "sonnet"'` on stdout
    - Pinned JSON (tool_input.model already present) → stdout byte-identical to stdin (diff -q)
    - No .planning/config.json in the sandbox cwd → passthrough (stdout == stdin) + non-empty stderr warn
    - EDGE-002: config.json present but no model_overrides key → passthrough + stderr warn
    - EDGE-001: non-Agent tool JSON (tool_name="Bash") → stdout byte-identical, no model touched
    - AC-002: DELEGATION_ENFORCER=off with an unpinned Agent JSON + config → passthrough (stdout == stdin)
    - Malformed JSON (not valid json) on stdin → status 0, passthrough
  </behavior>
  <action>
    Write the bats file first and confirm it FAILS (script absent) before Task 2. Use here-strings
    (`run bash scripts/hooks/delegation-enforcer.sh <<<"$JSON"`) as in spec PATH-001. Build fixtures
    with a helper that writes .planning/config.json into a bats tmpdir and `cd`s there so the hook's
    relative config lookup resolves. Assert byte-identical passthrough with `[ "$output" = "$input" ]`
    or a diff against the piped input. Use `jq -e` for the injection assertion. Cover all seven
    behaviors above as separate @test blocks. Do NOT mock the script under test (testing-policy:
    stub external state via the tmpdir/config only).
  </action>
  <verify>
    <automated>bats tests/bats/delegation-enforcer.bats  # RED: fails until Task 2</automated>
  </verify>
  <acceptance_criteria>bats file exists with 7 @test blocks; run fails only because the script is absent (RED proven), not on bats syntax errors.</acceptance_criteria>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: GREEN — delegation-enforcer.sh (fail-open, advisory model injection)</name>
  <files>scripts/hooks/delegation-enforcer.sh</files>
  <read_first>
    - scripts/hooks/gsd-phase-evidence-gate.sh (exact stdin/python3-heredoc pattern to copy)
    - .planning/config.json (model_overrides shape)
  </read_first>
  <behavior>
    - Makes every @test in tests/bats/delegation-enforcer.bats pass (GREEN)
  </behavior>
  <action>
    Create scripts/hooks/delegation-enforcer.sh with `#!/usr/bin/env bash`, a header comment block
    (name — one-line purpose, WHY paragraph, `# Usage:` line) and `set -euo pipefail` (hook convention,
    RESEARCH Pattern 5). First line of logic: `[ "${DELEGATION_ENFORCER:-}" = "off" ] && { cat; exit 0; }`
    for the AC-002 kill-switch (echo stdin untouched). Then `INPUT=$(cat)` and a `python3 - "$INPUT"
    <<'PY' ... PY` block. In python: json.loads inside try/except → on any exception print INPUT verbatim
    and sys.exit(0) (fail-open, RESEARCH threat: hostile stdin JSON). If data.get("tool_name") not in
    {"Agent","Task"} → print INPUT verbatim, sys.exit(0) (EDGE-001). If tool_input already has a truthy
    "model" → print INPUT verbatim, sys.exit(0) (byte-identical passthrough). Otherwise read subagent_type;
    load .planning/config.json — on missing file OR missing "model_overrides" key, warn to stderr and print
    INPUT verbatim, exit 0 (AC-001 no-config, EDGE-002). If model_overrides[subagent_type] resolves, set
    tool_input["model"] and print json.dumps(data); else warn + passthrough. Exit 0 in all non-usage cases.
    Emit the injected result as the full input envelope with tool_input.model set (the PATH-001 shape) — do
    NOT wrap in hookSpecificOutput this phase (see objective Open-Question-1 decision). Run shellcheck and
    fix all findings.
  </action>
  <verify>
    <automated>bats tests/bats/delegation-enforcer.bats && shellcheck scripts/hooks/delegation-enforcer.sh</automated>
  </verify>
  <acceptance_criteria>All delegation-enforcer.bats tests pass; shellcheck exits 0; hook exits 0 for every delegation decision.</acceptance_criteria>
</task>

</tasks>

<artifacts_produced>
New symbols/files this plan creates (exclude from cross-plan source-drift verification):
- `scripts/hooks/delegation-enforcer.sh` (new PreToolUse hook)
- `tests/bats/delegation-enforcer.bats` (new test file)
- env var contract: `DELEGATION_ENFORCER=off` kill-switch
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sub-agent transcript → hook stdin | Agent/Task tool_use JSON is attacker-influenceable in principle (a compromised sub-agent payload) |
| local repo → hook | `.planning/config.json` is a local file; malformed/absent must fail-open |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-DE-01 | Tampering | delegation-enforcer.sh stdin JSON parse | high | mitigate | `json.loads` in try/except → print INPUT verbatim + exit 0 (fail-open); never crash, never block (mirrors gsd-phase-evidence-gate.sh) |
| T-01-DE-02 | Denial of Service | advisory hook blocking a spawn | medium | mitigate | exit 0 in ALL non-usage cases; a contract mismatch degrades to today's behavior (passthrough), never a hard stop (plan.md Risk 1) |
| T-01-DE-03 | Tampering | malformed/absent config.json | low | mitigate | missing file or missing model_overrides key → stderr warn + passthrough (AC-001, EDGE-002); config is read-only, never written |
| T-01-SC | Tampering | package installs | low | accept | No package installs this phase — bash + Python stdlib only (RESEARCH Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `bats tests/bats/delegation-enforcer.bats` exits 0 (happy + error paths)
- `shellcheck scripts/hooks/delegation-enforcer.sh` exits 0 (REQ-12 cross-cutting)
- Manual spot: piping the spec PATH-001 unpinned JSON with a seeded config yields `.tool_input.model` set; piping a pinned JSON returns it byte-for-byte.
</verification>

<success_criteria>
- REQ-01 satisfied: inject / byte-identical passthrough / no-config warn / exit-0 all covered and green.
- REQ-02 satisfied: `DELEGATION_ENFORCER=off` passthrough covered and green.
- shellcheck-clean; no `.claude/settings.json` or SKILL.md touched.
</success_criteria>

<output>
Create `.planning/phases/01-levers/01-01-SUMMARY.md` when done.
</output>
