---
phase: 01-levers
plan: 05
type: tdd
wave: 1
depends_on: []
files_modified:
  - scripts/harness-audit.py
  - tests/test_harness_audit.py
autonomous: true
requirements: [REQ-08]

must_haves:
  truths:
    - "Scores 0-100 across four dimensions: dangling skill links, vendored-copy version drift, dead model pins in .planning/config.json, hook-registration drift (AC-008)"
    - "Dangling-symlink fixture → a finding + a score deduction (<100) (PATH-005, plan.md Unit Test List)"
    - "Version-drift fixture (installed skill 2.3.0 vs packaged 2.2.0) → a drift finding (plan.md Unit Test List)"
    - "Clean fixture → score 100, empty findings, exit 0 (plan.md Unit Test List)"
    - "--json emits a stable schema: {score, findings[].{kind,path,detail}} (plan.md Unit Test List)"
    - "No ~/.claude/skills present → score 100 with a `skipped: no harness dir` note — absence is not drift (EDGE-007)"
    - "Never blocks preflight — advisory only, exit 0 (AC-008)"
  artifacts:
    - path: "scripts/harness-audit.py"
      provides: "0-100 harness health scorer with --json output"
      min_lines: 60
    - path: "tests/test_harness_audit.py"
      provides: "pytest fixtures: dangling link, version drift, clean, json schema, no-harness-dir"
      contains: "harness-audit"
  key_links:
    - from: "scripts/harness-audit.py"
      to: "lib/agents_manifest.py::_parse_frontmatter"
      via: "reuses the regex frontmatter reader to read `version:` from SKILL.md (no PyYAML)"
      pattern: "frontmatter"
  prohibitions:
    - "Does NOT add a PyYAML (or any third-party) dependency — reuse the regex frontmatter approach from lib/agents_manifest.py (RESEARCH Anti-Patterns)"
    - "Does NOT edit skills/preflight/SKILL.md — the advisory section is Phase 2"
    - "Never returns a blocking exit code — advisory only (AC-008)"
---

<objective>
Build `scripts/harness-audit.py`: a standalone Python scorer (0-100) that grades the agent
harness itself — dangling skill links, vendored-copy version drift, dead model pins, and
hook-registration drift — with `--json` output, so an unattended run never starts on a
silently broken harness (US5). Advisory only: it never blocks. Preflight wiring is Phase 2.

Purpose: FFS grades environments but never the harness (RESEARCH Architectural Responsibility
Map). Scoring + JSON output mirrors gates.py's own rationale.

Output: harness-audit.py + its pytest fixture coverage (dangling link, drift, clean, json
schema, no-harness-dir).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@specs/003-orchestration-hardening/spec.md
@lib/agents_manifest.py
@.planning/config.json

<interfaces>
<!-- Executor: reuse these — no PyYAML, no new import-by-path decision. -->

Frontmatter reader to reuse (lib/agents_manifest.py:80-100 `_parse_frontmatter`):
  regex-based, stdlib only; reads name/version between the first two `---`; an UNCLOSED
  block yields {} (guards fence-less body text). Import or copy this approach — do NOT add PyYAML.

Test import-by-path helper to copy (lib/tests/test_gates.py:15-17):
  `spec = importlib.util.spec_from_file_location(name, DIR / f"{name}.py")`
  loads a module by file path (avoids the `lib/` package-vs-not question). Use the same
  approach to load scripts/harness-audit.py in the test.

Python conventions (RESEARCH Standard Stack): stdlib only (argparse/json/os/re/hashlib/pathlib);
`.planning/config.json` model_overrides is the source of truth for dead-pin detection.

--json schema (plan.md Unit Test List): {"score": int, "findings": [{"kind": str, "path": str, "detail": str}]}
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED — test_harness_audit.py fixtures (dangling / drift / clean / json / no-dir)</name>
  <files>tests/test_harness_audit.py</files>
  <read_first>
    - lib/tests/test_gates.py:8-23 (importlib load-by-path helper to copy)
    - specs/003-orchestration-hardening/spec.md — AC-008, PATH-005, EDGE-007
    - tests/test_agent_catalog.py (top-level pytest precedent in this repo)
  </read_first>
  <behavior>
    - dangling-symlink fixture harness dir → findings include a `dangling`-kind entry AND score < 100
    - version-drift fixture (installed SKILL.md version 2.3.0 vs packaged 2.2.0) → findings include a `drift`-kind entry
    - clean fixture (all links resolve, versions match) → score == 100, findings == [], exit 0
    - --json output parses and matches schema {score:int, findings:[{kind,path,detail}]}
    - no harness dir at all (point HOME at an empty tmp) → score == 100 with a note `skipped: no harness dir` (EDGE-007), exit 0
  </behavior>
  <action>
    Write tests/test_harness_audit.py FIRST; confirm RED (script absent). Build fixtures under tmp_path:
    a fake harness dir with a `skills/<n>/SKILL.md` symlink pointing at a missing target (dangling); a
    packaged copy + an installed copy with mismatched `version:` frontmatter (drift); and a clean set.
    Load scripts/harness-audit.py via the importlib-by-path helper copied from test_gates.py, OR invoke it
    as a subprocess (`python3 scripts/harness-audit.py --json`) and parse stdout — pick subprocess for the
    --json schema assertion so the CLI contract is exercised end-to-end. Do NOT mock filesystem calls —
    use real tmp dirs (testing-policy mock-minimization ladder rung 1). Assert score, finding kinds, and
    JSON schema keys. Cover EDGE-007 by pointing the audit at an empty/absent harness dir.
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_harness_audit.py -q  # RED: fails until Task 2</automated>
  </verify>
  <acceptance_criteria>test file exists with the 5 fixtures; run fails only because the script is absent (RED proven).</acceptance_criteria>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: GREEN — harness-audit.py (0-100 scorer, --json, advisory)</name>
  <files>scripts/harness-audit.py</files>
  <read_first>
    - lib/agents_manifest.py:80-100 (_parse_frontmatter to reuse)
    - .planning/config.json (model_overrides for dead-pin detection)
  </read_first>
  <behavior>
    - Makes every test in tests/test_harness_audit.py pass (GREEN)
  </behavior>
  <action>
    Create scripts/harness-audit.py (stdlib only: argparse, json, os, re, pathlib; import or copy
    _parse_frontmatter from lib/agents_manifest.py — no PyYAML, RESEARCH Anti-Patterns). Score starts at
    100; each finding deducts. Four checks: (1) dangling skill links — for each ~/.claude/skills/*/SKILL.md
    that is a symlink to a missing target, emit a `dangling` finding; (2) vendored-copy drift — compare
    installed skill `version:` frontmatter vs the repo/packaged copy, emit `drift` on mismatch; (3) dead
    model pins — flag .planning/config.json model_overrides values that are not recognized model aliases;
    (4) hook-registration drift — hooks present on disk but not registered (or vice versa). Emit findings as
    {kind, path, detail}. If the harness dir (~/.claude/skills) is absent, return score 100 with a note
    `skipped: no harness dir` and DO NOT treat absence as drift (EDGE-007). Support `--json` printing
    {"score", "findings"} to stdout; default (non-json) prints a human summary. Always exit 0 — advisory,
    never blocks. Keep functions <50 lines; make it importable (guard the CLI under `if __name__ ==
    "__main__"`) so the test can load it by path.
  </action>
  <verify>
    <automated>python3 -m pytest tests/test_harness_audit.py -q</automated>
  </verify>
  <acceptance_criteria>All test_harness_audit.py tests pass; --json schema stable; clean fixture → 100; no-harness-dir → 100 + skipped note; exit 0 always.</acceptance_criteria>
</task>

</tasks>

<artifacts_produced>
New symbols/files this plan creates (exclude from cross-plan source-drift verification):
- `scripts/harness-audit.py` (new standalone scorer)
- `tests/test_harness_audit.py` (new pytest file)
- CLI contract: `harness-audit.py [--json]`
- --json schema: `{score:int, findings:[{kind,path,detail}]}`
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| installed harness dir → auditor | ~/.claude/skills contents (symlinks, frontmatter) are read-only inputs; absence must not be misread as drift |
| .planning/config.json → auditor | local config read for dead-pin detection; malformed must fail-soft |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-HA-01 | Denial of Service | audit blocking preflight | high | mitigate | advisory only — always exit 0; preflight consumes the score, never gates on the process rc (AC-008) |
| T-01-HA-02 | Tampering | unclosed/hostile SKILL.md frontmatter | medium | mitigate | reuse `_parse_frontmatter` (returns {} on unclosed block — already hardened against body-text injection, agents_manifest.py docstring) |
| T-01-HA-03 | Tampering | absent harness dir scored as failure | low | mitigate | EDGE-007: absent ~/.claude/skills → score 100 + `skipped` note, not a deduction |
| T-01-SC | Tampering | package installs | low | accept | No package installs this phase — Python stdlib only (RESEARCH Package Legitimacy Audit: N/A) |
</threat_model>

<verification>
- `python3 -m pytest tests/test_harness_audit.py -q` exits 0 (all fixtures)
- Spot: `python3 scripts/harness-audit.py --json` against a clean fixture prints `"score": 100` and `"findings": []`, exit 0.
- No new third-party import (grep the script for pyyaml/yaml — absent).
</verification>

<success_criteria>
- REQ-08 (lever half) satisfied: four scoring dimensions + --json + advisory exit-0 + EDGE-007 all green.
- No preflight SKILL.md touched (advisory section is Phase 2); no PyYAML added.
</success_criteria>

<output>
Create `.planning/phases/01-levers/01-05-SUMMARY.md` when done.
</output>
