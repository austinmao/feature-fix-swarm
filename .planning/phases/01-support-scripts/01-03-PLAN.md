---
phase: 01-support-scripts
plan: 03
type: tdd
wave: 1
depends_on: []
files_modified:
  - scripts/gsd/state-phase.sh
  - tests/state-phase.bats
autonomous: true
gap_closure: true
requirements: [REQ-02]
must_haves:
  truths:
    - "bash scripts/gsd/state-phase.sh .planning/STATE.md — the literal ROADMAP Success Criterion 2 command, run against the REAL repo STATE.md — prints a single integer on stdout and exits 0"
    - "A bats fixture mirroring the real gsd-core 1.6.1 body ('Phase: 01 (support-scripts) — EXECUTING' + 'Plan: 1 of 2' + 'Status: Executing Phase 01') yields exit 0 and output exactly '0'"
    - "Zero-padded body phase numbers (e.g. 'Phase: 02', 'Phase: 08') are normalized to plain decimal output and never hit bash octal-arithmetic errors"
    - "All previously passing behaviors preserved: legacy 'Phase: 3 of 10' fixtures still yield their body-derived integers, missing file still exits 2 with 'not found', two-or-more args still exits 2 with usage, a body with no Phase line still exits 1 with an actionable stderr message"
    - "shellcheck -S warning stays clean on the full CI glob; bats tests/consent-check.bats still 7 ok; python3 -m pytest lib/tests -q still 190 passed"
  artifacts:
    - "scripts/gsd/state-phase.sh"
    - "tests/state-phase.bats"
  key_links:
    - "state-phase.sh body grep stays anchored at ^Phase: but no longer requires an 'of Y' tail, so the real 'Phase: NN (name) — STATUS' template line matches; the 'Plan: X of Y' line is never used as the phase source"
    - "awk frontmatter strip is unchanged and still runs before any grep, so frontmatter counters remain structurally unreadable"
    - "Completed-phase rule A2 unchanged: COMPLETED = CURRENT when the body Status line contains 'phase complete' case-insensitively, else CURRENT - 1 floored at 0"
---

<objective>
Close the single verification gap from 01-VERIFICATION.md (REQ-02 / ROADMAP Success
Criterion 2): `scripts/gsd/state-phase.sh` currently greps the STATE.md body for a
`Phase: X of Y` line that the real gsd-core 1.6.1 template never emits, so the
literal command `bash scripts/gsd/state-phase.sh .planning/STATE.md` exits 1 with no
integer. The real `## Current Position` body writes `Phase: 01 (support-scripts) —
EXECUTING` with progress on a separate `Plan: 1 of 2` line.

Purpose: FFS preflight and gates need the completed-phases integer from the STATE.md
that gsd actually writes, not from a fabricated fixture format. The 8 existing bats
tests pass against synthetic fixtures only — this plan makes the tests mirror the
real template so the suite can never again be green while the real artifact fails.

Output: updated `scripts/gsd/state-phase.sh` (loosened body regex, zero-pad-safe
extraction, corrected stderr vocabulary) and `tests/state-phase.bats` extended to 10
tests with real-template fixtures.
</objective>

## Artifacts this phase produces

- `scripts/gsd/state-phase.sh` — MODIFIED (no new file): body Phase grep loosened to `^Phase:[[:space:]]*[0-9]+` (drops the `of Y` requirement so the real `Phase: NN (name) — STATUS` line matches); CURRENT extraction sed becomes `s/^Phase:[[:space:]]*0*([0-9]+).*/\1/` (leading-zero strip — zero-padded phase numbers can never trigger bash octal arithmetic and stdout is always plain decimal); exit-1 stderr message rephrased to `state-phase: no 'Phase: <number>' line found in <path> body`
- `tests/state-phase.bats` — MODIFIED: two new hermetic fixture tests (10 total) — real-template mid-phase fixture asserting exit 0 + output `0`, and real-template phase-complete fixture with zero-padded `Phase: 02` asserting exit 0 + output `2`; the no-Phase-line test's message assertion updated to the new stderr vocabulary
- New stderr message string introduced: `no 'Phase: <number>' line found` (replaces the prior no-match message)
- No new files, functions, CLI flags, or packages; exit-code contract (0/1/2) unchanged

<execution_context>
@/Users/luminamao/Documents/Github/feature-fix-swarm/.claude/worktrees/gsd-ruflo/.claude/gsd-core/workflows/execute-plan.md
@/Users/luminamao/Documents/Github/feature-fix-swarm/.claude/worktrees/gsd-ruflo/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-support-scripts/01-VERIFICATION.md
@.planning/phases/01-support-scripts/01-02-SUMMARY.md
@scripts/gsd/state-phase.sh
@tests/state-phase.bats
</context>

<feature>
  <name>state-phase.sh parses the real gsd-core 1.6.1 STATE.md body format</name>
  <files>scripts/gsd/state-phase.sh, tests/state-phase.bats</files>
  <behavior>
    Gap-closure contract, exhaustively (everything not listed here is unchanged from
    the shipped 01-02 behavior):
    - The body Phase grep matches any body line starting `Phase:` followed by
      optional whitespace and digits — regex `^Phase:[[:space:]]*[0-9]+` — so BOTH
      the real template line `Phase: 01 (support-scripts) — EXECUTING` AND the
      legacy fixture line `Phase: 3 of 10` match. First match wins (head -1).
    - CURRENT is the leading integer with leading zeros stripped: `01` -> 1,
      `08` -> 8, `3` -> 3, `10` -> 10. The mid-phase arithmetic
      COMPLETED=CURRENT-1 (floored at 0) and the phase-complete branch
      COMPLETED=CURRENT both therefore emit plain decimal (never `02`, never an
      octal error on `08`/`09`).
    - The `Plan: X of Y` line in the real body is NOT a phase source: the grep is
      anchored at ^Phase: so `Plan: 1 of 2` can never match.
    - Completed-phase rule unchanged (locked decision A2 from STATE.md decisions
      log): first body `^Status:` line containing `phase complete` case-insensitively
      -> COMPLETED=CURRENT; otherwise COMPLETED=CURRENT-1 floored at 0. Against the
      real repo STATE.md (`Phase: 01 ... — EXECUTING`, `Status: Executing Phase 01`)
      the output is `0` with exit 0.
    - No-Phase-line branch keeps exit 1 but the message now describes what is
      actually searched for: `state-phase: no 'Phase: <number>' line found in
      <path> body` on stderr.
    - Missing-file exit 2, usage exit 2, awk frontmatter strip, stdout shape
      (single integer line, nothing else): all unchanged.
  </behavior>
  <implementation>
    Three-line surgical change to scripts/gsd/state-phase.sh (grep pattern, stderr
    message, sed pattern) — the awk strip, arg guards, Status tie-breaker, and
    floor-at-zero logic are already correct and must not be restructured. Tests
    first (RED): the two new real-template fixtures fail against the shipped script
    (it exits 1 on them), and the updated message assertion fails too; then the
    script change turns the whole 10-test suite green. House style holds:
    #!/usr/bin/env bash, set -uo pipefail (no -e), bash-3.2-safe, all expansions
    double-quoted, grep -E only (never -P), no subprocesses beyond awk/grep/sed/head.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — add real-template fixtures to tests/state-phase.bats</name>
  <files>tests/state-phase.bats</files>
  <read_first>
    - tests/state-phase.bats (the suite being extended — reuse its exact REPO_ROOT/SCRIPT header, quoted-heredoc fixture mechanics, and run/[ "$status" -eq N ]/[ "$output" = "N" ] assertion shapes)
    - .planning/STATE.md (the REAL artifact whose '## Current Position' body the new fixtures must mirror line-for-line: 'Phase: 01 (support-scripts) — EXECUTING', 'Plan: 1 of 2', 'Status: Executing Phase 01', 'Last activity: ...')
    - .planning/phases/01-support-scripts/01-VERIFICATION.md (gaps frontmatter — the three 'missing' items this plan exists to close)
    - scripts/gsd/state-phase.sh (current shipped script — confirms why the new fixtures fail RED: its body grep requires an 'of Y' tail)
  </read_first>
  <behavior>
    - New Test 9 (real-template mid-phase): fixture with poisoned frontmatter (completed_phases: 999, percent: 999) and a body copying the real STATE.md Current Position section — '## Current Position' heading, blank line, 'Phase: 01 (support-scripts) — EXECUTING' (em dash, as in the real file), 'Plan: 1 of 2', 'Status: Executing Phase 01', 'Last activity: 2026-07-05 — Phase 01 execution started' -> exit 0, output exactly "0"
    - New Test 10 (real-template phase-complete, zero-padded): same real-template shape but 'Phase: 02 (support-scripts) — EXECUTING', 'Plan: 2 of 2', 'Status: Phase complete — ready for verification' -> exit 0, output exactly "2" (locks BOTH the A2 complete branch under the real format AND leading-zero normalization: '02' must print as '2')
    - Updated Test 6 (body without Phase line): fixture unchanged; the message assertion becomes a substring match on "no 'Phase: <number>'" (unique to the NEW stderr vocabulary — the shipped script's old message does not contain the "<number>" token, so this test fails RED), still asserting exit 1
    - All 7 other existing tests remain byte-for-byte untouched and must still pass in GREEN
  </behavior>
  <action>
    Extend tests/state-phase.bats (do NOT rewrite the file — append the two new
    @test blocks after the existing ones and edit only Test 6's message assertion
    line). Fixture mechanics identical to the existing suite: each test writes its
    own STATE.md to "$BATS_TEST_TMPDIR" via a quoted heredoc, then
    run bash "$SCRIPT" "$FIXTURE".

    Test 9 fixture body must be copied from the real .planning/STATE.md Current
    Position section (read it first), including the em-dash character and the
    'Plan: 1 of 2' line — the Plan line's presence is load-bearing: it proves the
    script derives the phase number from the 'Phase:' line and never from
    'Plan: X of Y'. Frontmatter carries the poisoned counters (completed_phases:
    999, percent: 999) per the suite's established regression pattern. Assert
    [ "$status" -eq 0 ] and [ "$output" = "0" ] (exact equality, not substring).

    Test 10 fixture uses the same real-template line shapes with 'Phase: 02
    (support-scripts) — EXECUTING' and 'Status: Phase complete — ready for
    verification'. Assert [ "$status" -eq 0 ] and [ "$output" = "2" ] — exact
    equality is the leading-zero-normalization proof ('02' printed verbatim would
    fail this assertion).

    Test 6 edit: replace its output-substring assertion with
    [[ "$output" == *"no 'Phase: <number>'"* ]]. The "<number>" token is what makes
    this substring unique to the NEW message — a looser prefix like "no 'Phase:"
    would also match the shipped script's old message and Test 6 would not go RED.
    Keep its fixture, name, and exit-1 assertion exactly as they are.

    Do NOT touch scripts/gsd/state-phase.sh in this task. RED gate: the suite must
    now fail with exactly 3 failing tests (Tests 9 and 10 because the shipped
    script exits 1 on their real-template fixtures, Test 6 because the shipped
    script's old message lacks the "<number>" token) and 7 passing.
    Commit as: test(01-03): add real STATE.md template fixtures for state-phase.sh
  </action>
  <verify>
    <automated>! bats tests/state-phase.bats</automated>
  </verify>
  <acceptance_criteria>
    - grep -c '@test' tests/state-phase.bats returns 10
    - grep -n 'Plan: 1 of 2' tests/state-phase.bats finds a match (real-template Plan line present in the Test 9 fixture)
    - grep -n 'EXECUTING' tests/state-phase.bats finds at least two matches (Tests 9 and 10 real-template Phase lines)
    - grep -n 'Phase: 02' tests/state-phase.bats finds a match (zero-padded fixture present)
    - bats tests/state-phase.bats exits non-zero, and `bats tests/state-phase.bats 2>&1 | grep -c '^not ok'` returns 3 (Tests 6, 9, 10 fail RED; the other 7 still pass)
    - git diff --name-only shows tests/state-phase.bats and NOT scripts/gsd/state-phase.sh
  </acceptance_criteria>
  <done>10-test hermetic suite exists with real-template fixtures failing RED against the shipped script, proving the suite can now detect the real-artifact mismatch that 01-VERIFICATION.md caught</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — loosen state-phase.sh body regex to the real template and re-run the literal ROADMAP command</name>
  <files>scripts/gsd/state-phase.sh</files>
  <read_first>
    - scripts/gsd/state-phase.sh (the file being modified — the change is 3 lines inside the PHASE_LINE/CURRENT block; everything else stays)
    - tests/state-phase.bats (the 10-test contract from Task 1 — implementation must satisfy it exactly)
    - .planning/STATE.md (the real artifact the literal ROADMAP command runs against; expected live output is 0 while Phase 01 is mid-execution)
  </read_first>
  <action>
    Make the minimal three-line change to scripts/gsd/state-phase.sh:

    1. PHASE_LINE grep: change the pattern to '^Phase:[[:space:]]*[0-9]+' —
       anchored at ^Phase: with optional whitespace and digits, no tail
       requirement, so both 'Phase: 01 (support-scripts) — EXECUTING' and the
       legacy 'Phase: 3 of 10' match. Keep grep -E | head -1 unchanged.
    2. No-match stderr message: change to
       "state-phase: no 'Phase: <number>' line found in $STATE_FILE body"
       (exit 1 branch otherwise unchanged).
    3. CURRENT sed: change the substitution to
       's/^Phase:[[:space:]]*0*([0-9]+).*/\1/' — the 0* before the capture group
       strips leading zeros so '01' extracts as 1 and '08' as 8, keeping the
       $((CURRENT - 1)) arithmetic octal-safe and the phase-complete branch's
       COMPLETED=$CURRENT output plain decimal. (A body of 'Phase: 0 ...' still
       extracts 0: the capture group requires at least one digit, so sed
       backtracks rather than emptying the capture.)

    Do NOT touch: the shebang/set line, arg guard, STATE_FILE default,
    missing-file exit-2 branch, awk frontmatter strip, Status-line grep, the A2
    tie-breaker (phase-complete -> CURRENT, else CURRENT-1 floored at 0), or the
    final printf. The header comment block may be left as-is except that if any
    line describes the old required line shape it should be updated to say the
    script reads the leading integer of the body's 'Phase:' line.

    Run until green: bats tests/state-phase.bats (10 ok), shellcheck -S warning
    scripts/gsd/state-phase.sh, then the literal ROADMAP Success Criterion 2
    command from the repo root: bash scripts/gsd/state-phase.sh .planning/STATE.md
    — it must print a single integer (currently 0) and exit 0. Also re-run the
    regression gates: bats tests/consent-check.bats (7 ok) and
    python3 -m pytest lib/tests -q (190 passed).
    Commit as: fix(01-03): parse real gsd-core STATE.md body format in state-phase.sh
  </action>
  <verify>
    <automated>bats tests/state-phase.bats && shellcheck -S warning scripts/gsd/state-phase.sh && bash scripts/gsd/state-phase.sh .planning/STATE.md | grep -qE '^[0-9]+$'</automated>
  </verify>
  <acceptance_criteria>
    - bats tests/state-phase.bats exits 0 with 10 tests passing
    - bash scripts/gsd/state-phase.sh .planning/STATE.md prints a line matching the extended regex ^[0-9]+$ on stdout and exits 0 (the literal ROADMAP Success Criterion 2 command against the REAL repo STATE.md — closes the failed truth)
    - shellcheck -S warning scripts/gsd/state-phase.sh exits 0 with no findings
    - shellcheck -S warning setup.sh hooks/*.sh scripts/*.sh scripts/hooks/*.sh scripts/harness/*.sh scripts/gsd/*.sh exits 0 (full CI glob stays green)
    - bash scripts/gsd/state-phase.sh /nonexistent/STATE.md exits 2 and stderr contains "not found" (preserved behavior)
    - bash scripts/gsd/state-phase.sh a b exits 2 and stderr contains "usage: state-phase.sh" (preserved behavior)
    - grep -c "no 'Phase: <number>'" scripts/gsd/state-phase.sh returns 1 or more (new stderr vocabulary present in the script)
    - bats tests/consent-check.bats exits 0 with 7 tests passing (untouched suite unaffected)
    - python3 -m pytest lib/tests -q reports 190 passed (regression gate; this plan touches no Python)
  </acceptance_criteria>
  <done>ROADMAP Success Criterion 2 is live-verified: the literal command prints an integer and exits 0 against the real STATE.md; all 10 state-phase tests plus the 7 consent-check tests plus pytest 190 stay green; shellcheck clean on the full CI glob</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller argv → state-phase.sh | State-file path is caller-controlled (spaces, globs; invoked programmatically by FFS automation) |
| STATE.md content → parser | File content is semi-trusted; frontmatter counters are documented-unreliable and must remain structurally unreadable |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-07 | Tampering | Body-regex vs real-template mismatch (a silently wrong or failing integer misgates downstream FFS automation) | high | mitigate | Real-template fixture pair (Tests 9/10) mirrors the live gsd-core 1.6.1 body verbatim; Task 2 acceptance re-runs the literal ROADMAP command against the real .planning/STATE.md |
| T-01-08 | Denial of Service | Zero-padded phase number ('08'/'09') triggering a bash octal arithmetic error (script crash, no integer) | medium | mitigate | Leading-zero strip in the sed capture (Task 2 step 3); Test 10's exact-equality assertion on a zero-padded fixture pins normalized decimal output |
| T-01-05 | Tampering | Frontmatter counters mistaken for the body signal (carried from plan 01-02) | high | mitigate | awk strip untouched and still upstream of every grep; poisoned-counter fixtures (999s) retained in all new fixtures |
| T-01-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs zero packages; the script invokes no node/gsd-tools subprocess |
</threat_model>

<verification>
- `bash scripts/gsd/state-phase.sh .planning/STATE.md` — prints a single integer, exits 0 (the literal ROADMAP Success Criterion 2 command against the real repo STATE.md; this is the failed truth from 01-VERIFICATION.md and the primary proof of closure)
- `bats tests/state-phase.bats` — 10 tests pass, including the two real-template fixtures
- `bats tests/consent-check.bats` — still 7 tests pass (REQ-01 untouched)
- `shellcheck -S warning setup.sh hooks/*.sh scripts/*.sh scripts/hooks/*.sh scripts/harness/*.sh scripts/gsd/*.sh` — zero findings
- `python3 -m pytest lib/tests -q` — still 190 passed
</verification>

<success_criteria>
- ROADMAP Success Criterion 2 observably true against the REAL artifact: exit 0 + integer stdout from `bash scripts/gsd/state-phase.sh .planning/STATE.md`
- The bats suite can no longer be green while the real template fails: Tests 9/10 mirror the live gsd-core 1.6.1 Current Position body line-for-line
- Zero regressions: legacy fixtures, exit-2 missing-file, exit-2 usage, exit-1 no-Phase-line (with updated message), consent-check suite, pytest baseline, and the CI shellcheck glob all stay green
</success_criteria>

<output>
Create `.planning/phases/01-support-scripts/01-03-SUMMARY.md` when done
</output>
